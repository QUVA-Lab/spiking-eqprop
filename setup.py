from setuptools import setup, find_packages

setup(
    name = "spiking-eqprop",
    author = "peter",
    version = 0,
    install_requires = [''],
    dependency_links = (),
    scripts = [],
    packages=find_packages(),
    )
